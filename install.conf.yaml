- clean: ['~']

- link:
    ~/.config/nvim:
      create: true
      path: nvim

    ~/.config/themes:
      create: true
      path: themes

    ~/.config/tmux:
      create: true
      path: tmux

    ~/.config/kitty:
      create: true
      path: kitty

    ~/.zshrc:
      create: true
      path: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
