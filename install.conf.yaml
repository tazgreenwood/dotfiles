- clean: ['~']

- link:
    ~/.config/nvim:
      create: true
      path: nvim

    ~/.config/alacritty:
      create: true
      path: alacritty

    ~/.zshrc:
      create: true
      path: zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
